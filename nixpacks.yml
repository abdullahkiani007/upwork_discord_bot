# nixpacks.yml

python38:
  buildInputs:
    - gcc
  setup: |
    python -m venv --copies /opt/venv
    . /opt/venv/bin/activate
    pip install -r requirements.txt
  start: |
    . /opt/venv/bin/activate
    python main.py
